## Setup 

```sh
pip install -r requirements.txt

# to run a flask server for your docs then use the following command
python3 main.py
# then open in http://127.0.0.1:5000

# to run the program in cli use the following command
python3 main-cli.py
```